//========== Format the Date to date string ========
module.exports = {
    formatDate: (date) => {
      return date.toLocaleDateString();
    },
  };

  //=================================================